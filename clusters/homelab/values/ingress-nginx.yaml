# Per-cluster overrides for ingress-nginx
# Use LoadBalancer when MetalLB is configured.
ingress-nginx:
  controller:
    service:
      type: LoadBalancer
  # Expose Forgejo SSH via the ingress-nginx LoadBalancer (TCP service mapping).
  tcp:
    "22": "forgejo/forgejo-ssh:22"
