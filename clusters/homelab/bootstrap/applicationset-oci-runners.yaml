apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-oci-runners
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: forgejo-runner
            namespace: forgejo-runner
            repoURL: oci://codeberg.org/wrenix/helm-charts
            chart: forgejo-runner
            version: 0.4.28
            forgejoUrl: https://forgejo.k8s.magomago.moe
            configSecretName: forgejo-runner-config
  template:
    metadata:
      name: 'platform-{{name}}'
    spec:
      project: platform
      source:
        repoURL: '{{repoURL}}'
        chart: '{{chart}}'
        targetRevision: '{{version}}'
        helm:
          releaseName: '{{name}}'
          values: |
            runner:
              config:
                # Keep secrets out of Git: the registration token is provided via a Secret
                # (typically provisioned from Vault via External Secrets).
                create: false
                existingSecret: "{{configSecretName}}"
                instance: "{{forgejoUrl}}"
              labels:
                - ubuntu-latest
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
